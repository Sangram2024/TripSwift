import ReviewBooking from "@/components/bookingComponents/ReviewBooking";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <ReviewBooking />
    </div>
  );
};

export default page;
