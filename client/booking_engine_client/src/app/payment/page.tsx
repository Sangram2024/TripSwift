import MainPage from "@/components/payment/MainPage";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div className="">
      <MainPage />
    </div>
  );
};

export default page;
