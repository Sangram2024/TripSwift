import React from "react";
import RegisterForm from "./register-form";

type Props = {};

export default function Register({}: Props) {
  return (
    <section>
      <RegisterForm />
    </section>
  );
}
